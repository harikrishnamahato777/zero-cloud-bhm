apiVersion: v1
kind: Service
metadata:
  name: bhm-service
  labels:
    app: browser-history-manager
spec:
  selector:
    app: browser-history-manager
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
